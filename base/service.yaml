apiVersion: "v1"
kind: "Service"
metadata:
  name: "it-request"
  namespace: "production"
spec:
  selector:
    app: "it-request"
  ports:
  - protocol: "TCP"
    port: 3001
    targetPort: 3001
    name: "http"
  type: "ClusterIP"