apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
replicas:
  - name: it-request
    count: 1
labels:
  - includeSelectors: true
    pairs:
      app: it-request
configMapGenerator:
  - behavior: replace
    envs:
      - .env
    name: it-request-config
    namespace: production
resources:
  - ../../base
images:
  - name: tanpiyangkoon/it-request
    newTag: production-3b8d1504611-54
